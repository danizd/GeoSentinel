[project]
name = "geosentinel"
version = "0.1.0"
description = "Sistema de agregacion y monitorizacion de incidentes geoespaciales en tiempo real"
requires-python = ">=3.12"
dependencies = [
    "websockets>=16.0",
]

[tool.uv]
package = false
